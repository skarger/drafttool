import Ember from 'ember';

export default Ember.Component.extend({
  title: "watch list",
  players: [Player.create({name: "tom brady"})]
 // var player_one = Player.create();
 // player_one.name = 'tom brady';
 // player_one.team = 'ne';
 // players.add(player_one);

//  addPlayer: function(name) {
//    player.push(name)
//  }
//
});
